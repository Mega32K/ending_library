# Use the supplied Blockbench visual language for the camera editor

The rewritten Camera Animation Editor follows the supplied Blockbench reference as its primary visual direction: dark charcoal and near-black workspace surfaces, restrained layered panel contrast, compact light text, subtle separators, compact top-level menus, and clear blue active-state feedback. The default Standard Dark profile and the optional High-Contrast Dark profile both use this semantic visual language. The File, Edit, Animation, Keyframe, Timeline, Tools, View, Help, and Settings command surfaces should feel like a professional desktop animation application rather than a vanilla Minecraft screen.

The user's written clarifications are authoritative over the screenshot. In particular, a top-level menu may be open while one submenu item is hovered and rendered in a blue tone; a selected animation diamond node changes to a blue tone; and these are active-state examples to reproduce consistently across the editor. The implementation should use semantic color tokens rather than scattered literal colors, with bounded contrast and no excessive glow, transparency, or scaling.

The supplied timeline screenshot additionally establishes the bottom panel hierarchy: compact header and tool strip, time/frame readout, resizable track tree, synchronized ruler and lanes, restrained row separators, compact transport controls, horizontal scrolling, and a blue playhead line. Its persistent row-local visibility and add buttons are not copied because the user's written Clean Timeline Surface requirement remains authoritative.

A separate compact Editor Status Bar sits below the Timeline Panel. It uses the same restrained charcoal layering, separators, compact typography, semantic icons, and blue interaction language, but remains visually distinct from timeline transport and track controls. At narrow widths it collapses text into meaningful badges and a status drawer before reducing the Minimum Usable Region of either the Live World Viewport or Timeline Panel.

Blue selection feedback applies to hover, focus, selection, and active navigation according to state, but the states remain distinguishable through row shape, border, icon, text weight, or indicator placement. Collaborator colors, Auto Key, warning, error, and synchronization states must remain semantically distinct and must not depend on color alone. All colors come from semantic tokens, and the High-Contrast Dark profile increases contrast without excessive glow or scale changes. The palette is inspired by Blockbench without requiring a pixel-perfect clone or reusing Minecraft's default GUI palette.
