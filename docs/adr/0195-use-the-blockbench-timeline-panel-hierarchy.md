# Use the supplied Blockbench timeline hierarchy as the panel reference

The bottom Timeline Panel and its default left-side Timeline Track Tree Surface follow the supplied Blockbench reference's visual hierarchy: a dark compact panel header, a narrow command strip, a current-time and display-frame readout, a synchronized Camera Track Tree companion surface, a top ruler, subdued row separators, a broad lane canvas, a blue playhead head and vertical line, compact diamond nodes, and restrained horizontal scrolling. In the default workspace the track tree occupies the lower region of the Left Animation Navigation Stack and remains row-aligned with the bottom lane canvas rather than being fused with the Animation Resource Browser. Surface colors, text, dividers, active blue, and disabled states come from the established semantic color tokens. The reference guides proportions and hierarchy rather than requiring a pixel-perfect clone or Minecraft absolute coordinates.

The left tool cluster keeps the established order: Graph Editor Mode (`F3`), Timeline Channel Visibility, Clear Selected Timeline Tracks, Timeline Population Command, create Command Animation Effect Event, and Set Marker Command. Particle and sound event tools are absent. The right transport cluster presents compact previous navigation, play or pause, and next navigation controls as in the reference; start/end seeking, loop control, exact seek, Work Range operations, and the terminal Runtime Stop alias remain available through the Timeline Menu and shortcuts. Every button is operational, focusable, and covered by the one-second Camera Editor Button Tooltip contract.

The Timeline Panel responds to available width and editor density without exaggerated scaling. Lower-priority actions move into a stable overflow menu before labels, hit targets, the time readout, ruler, or transport controls become unusable; the track tree has bounded relative resizing and may collapse to a restorable drawer in narrow layouts; ruler ticks adapt their density instead of overlapping; lane rows remain aligned across the tree and canvas; and Timeline Virtualization limits rendered rows and time geometry without altering off-screen content. The screenshot's persistent row-local visibility and add controls are not copied because the user's explicit Clean Timeline Surface rule takes precedence: track operations remain in the toolbar, context menu, shortcuts, or inspector, and hovering or selecting a row never reveals an action strip.
